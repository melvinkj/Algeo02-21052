import tkinter as tk
from PIL import ImageTk, Image

window = tk.Tk()

window.title('Face Recognition using Eigenface')
window.geometry("1200x720")

# imagePath = "./tkinter/image.jpg"
# imageMain = Image.open(imagePath)
# # imageMain = imageMain.resize((500, 500), Image.ANTIALIAS)

# # Create a photoimage object of the image in the path
# test = ImageTk.PhotoImage(imageMain)

# label = tk.Label(image=test)


# label.place(x = 0, y = 0)
# label.pack()


window.mainloop()